using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class rehabili : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int x = 1;
        Debug.Log("test");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
